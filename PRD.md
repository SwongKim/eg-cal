# PRD: 공학용 연산 도우미 (Engineering Calculation Assistant)

## 1. 개요

공학도 및 실험/분석 업무 종사자를 위한 웹 기반 도구. 상단의 공학용 계산기와 하단의 실험 데이터 통계 분석 도구를 하나의 페이지에서 제공하여, 계산과 데이터 분석을 오가는 번거로움을 줄인다.

- **개발 환경 URL**: http://localhost:8080
- **DB**: Firebase Authentication (로그인/인증 용도로만 사용, v1 범위)

## 2. 목표

- 실험 결과(검량선 데이터 등)를 입력하고 확인 버튼을 클릭하면 회귀분석 및 관련 통계량/그래프를 산출
- 산출된 모든 결과(수식, 수치, 표, 그래프)를 사용자가 보고서/논문 등 외부 문서에 바로 붙여넣을 수 있는 형태로 제공
- 계산기와 분석 도구를 한 화면에서 끊김 없이 사용

## 3. 타겟 사용자

- 공과대학 학부/대학원생
- 분석/품질(QC) 실험 담당자 (검량선, 회수율, 검출한계 산출이 필요한 경우)

## 4. 페이지 구성

### 4.1 상단 — 공학용 계산기

- 사칙연산, 삼각함수, 로그/지수, 거듭제곱/제곱근, 팩토리얼, 상수(π, e), 괄호, 메모리(M+, M-, MR, MC) 등 표준 공학용 계산기 기능
- **입력 방식**: 화면 버튼 클릭(마우스) + 물리 키보드 입력(숫자, 연산자, Enter, Backspace, 단축키) 모두 지원
- 계산 결과를 하단 테이블에 바로 삽입할 수 있는 연동 기능은 v2로 연기 (v1은 사용자가 결과를 보고 직접 입력)

### 4.2 중단 — 데이터 입력 테이블

- **레이아웃**: 2열(X, Y) 구조의 테이블, 세로 스크롤 지원 (행 개수 제한 없음, 예: 수백 개 데이터 포인트)
- **용도 예시**: 농도별 검출치, 입력 전압별 출력 전류 등 X-Y 쌍 데이터
- **기능**
  - 행 추가/삭제
  - 셀 단위 직접 입력 및 키보드 이동(Tab/Enter로 다음 셀 이동)
  - 엑셀/스프레드시트에서 복사한 표 데이터 붙여넣기 지원: 여러 행·2열(X, Y) 블록을 한 번에 붙여넣어도 각 셀에 값이 올바르게 분배되어 채워짐 (행이 부족하면 자동으로 행 추가)
  - 잘못된 값(비숫자, 빈 셀) 입력 시 시각적 경고
  - 데이터 입력 후 **"확인" 버튼 클릭 시** 하단 결과 산출 (실시간 자동 계산 아님)

### 4.3 하단 — 산출 결과

테이블에 데이터 입력 후 **확인 버튼**을 클릭하면 아래 항목을 산출한다.

**요청된 필수 항목**
| 구분 | 항목 |
|---|---|
| 회귀분석 | 회귀식 (기울기, 절편) |
| 검출/정량 한계 | LOD (검출한계), LOQ (정량한계) |
| 정확도 | Recovery (회수율) |
| 통계적 유의성 | 신뢰구간, 회귀 분산분석표(ANOVA), p-값, t-통계량 |
| 그래프 | 구간 그림(Interval Plot), 개별값 그림(Individual Value Plot), 상자 그림(Box Plot), 잔차 그림(Residual Plot) |

**추가 항목 (v1 확정)** — 분석화학/계측 검량선 분석에서 일반적으로 함께 제공되는 지표로, 가중회귀를 제외하고 모두 v1에 포함
| 구분 | 항목 | 비고 |
|---|---|---|
| 적합도 | R², 수정된 R² | 회귀식 설명력 |
| 오차 | 잔차표준편차(Syx), 표준오차(SE of slope/intercept) | LOD/LOQ 계산에 쓰이는 값이라 화면 표시 여부와 무관하게 항상 계산됨 |
| 예측 | 예측구간(Prediction Interval), 검량선 신뢰대역 포함 산점도 | 신뢰구간과 구분되는 개별 예측값 범위 |
| 선형성 검정 | Lack-of-fit test | 검량선이 진짜 선형인지 검정 |
| 등분산성 검정 | F-test 또는 Levene's test | 가중회귀 필요 여부 판단 |
| 잔차 정규성 | Shapiro-Wilk test | 잔차 그림 보완 |
| 이상치 검정 | Grubbs' test, Cook's distance | 이상 데이터 포인트 식별 |
| 정밀도 | %RSD (상대표준편차), 반복성 | |
| 가중회귀 옵션 | 1/x, 1/x² 가중치 | 이분산 데이터 대응 (v2 예정) |
| 상관계수 | Pearson r | |

### 4.4 결과 출력/복사 기능

- 모든 수치·통계표는 텍스트/표 형태로 "복사" 버튼 제공 (클립보드 복사, 필요 시 서식 있는 표 형태 + plain text 형태 둘 다 지원)
- 회귀식 등 수식은 텍스트(예: `y = 1.23x + 0.45`) 및 필요 시 이미지(수식 렌더링) 복사 지원
- 그래프는 이미지(PNG) 형태로 복사/다운로드 가능

## 5. 기술 스택 (초안)

- Frontend: 미정 (React 등 SPA 프레임워크 가정)
- Backend/DB: Firebase Authentication (로그인/인증 전용, v1). 사용자별 입력 이력/데이터 저장(Firestore 등)은 v2로 연기
- 개발 서버: http://localhost:8080

## 6. 비기능 요구사항

- 확인 버튼 클릭 시 계산 성능: 수백 행 데이터 입력 시에도 지연 없이 결과 산출
- 반응형 UI (데스크톱 우선, 모바일은 v2 고려)
- 브라우저 새로고침 시 입력 데이터 유지: v1은 로컬(브라우저) 저장만 사용, 서버 측 이력 저장은 v2

## 7. 미확정 사항 (확인 필요)

(현재 없음)

## 8. Out of Scope (v1)

- 모바일 앱
- 다국어 지원
- 협업(다중 사용자 동시 편집) 기능
- 사용자별 데이터 저장/이력 관리 (Firestore 등, v2 예정)
- 다중 시리즈(여러 검량선 동시 비교) (v2 예정)
- 가중회귀(1/x, 1/x² 가중치) (v2 예정)
- 전체 결과 리포트 CSV/파일 내보내기 (v2 예정, v1은 복사 기능만 제공)
- 계산기 결과를 테이블 셀에 자동 삽입하는 연동 기능 (v2 예정)
