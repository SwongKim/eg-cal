# Sitemap & 기능정의서 — 공학용 연산 도우미

> 본 문서는 `PRD.md`를 기준으로 서비스의 전체 페이지 구조(사이트맵)와 페이지별 기능을 정의한다. 범위(v1/v2)는 PRD와 동일하게 따른다.

## 1. 사이트맵 다이어그램

```
/ (루트)
├─ 미로그인 상태 → 랜딩 페이지 표시
└─ 로그인 상태   → /app 로 리다이렉트 (메인 도구 페이지)

/login            로그인 페이지
├─ /signup        회원가입 페이지
└─ /reset-password 비밀번호 재설정 페이지

/app              메인 도구 페이지 (인증 필요) ★핵심 페이지
├─ 상단: 공학용 계산기 영역
├─ 중단: 데이터 입력 테이블 영역
└─ 하단: 산출 결과 영역

/* (정의되지 않은 경로)  404 Not Found 페이지
```

## 2. 라우팅 표

| 경로 | 페이지명 | 인증 필요 | 버전 |
|---|---|---|---|
| `/` | 랜딩 페이지 | 불필요 (로그인 상태면 `/app`으로 리다이렉트) | v1 |
| `/login` | 로그인 | 불필요 (로그인 상태면 `/app`으로 리다이렉트) | v1 |
| `/signup` | 회원가입 | 불필요 | v1 |
| `/reset-password` | 비밀번호 재설정 | 불필요 | v1 |
| `/app` | 메인 도구 페이지 | **필요** | v1 |
| `/*` | 404 Not Found | - | v1 |

> v2 예정 페이지(마이페이지/입력 이력, 다중 시리즈 관리 등)는 5번 항목 참고.

---

## 3. 페이지별 기능 정의서

### 3.1 루트 (`/`)

- **목적**: 서비스 소개 랜딩 페이지. 로고/한 줄 소개, 기능 요약, 로그인·회원가입 버튼을 보여준다.
- **접근 조건**: 없음
- **기능**
  - Firebase Auth 로그인 상태 확인
  - 로그인 상태 → `/app`으로 자동 이동 (랜딩 화면을 그리지 않음)
  - 미로그인 상태 → 랜딩 화면 표시, "로그인"/"회원가입" 버튼으로 각 페이지 이동

### 3.2 로그인 페이지 (`/login`)

- **목적**: Firebase Authentication을 통한 사용자 로그인
- **접근 조건**: 미로그인 사용자만 접근 (로그인 상태면 `/app`으로 자동 이동)
- **UI 구성요소**
  - 이메일 입력란
  - 비밀번호 입력란
  - "로그인" 버튼
  - "회원가입" 페이지 이동 링크
  - "비밀번호를 잊으셨나요?" → `/reset-password` 이동 링크
- **기능**
  - 이메일/비밀번호 로그인 (Firebase Authentication)
  - 입력값 유효성 검사 (이메일 형식, 빈 값)
  - 로그인 실패 시 에러 메시지 표시 (예: 비밀번호 불일치, 존재하지 않는 계정)
  - 로그인 성공 시 `/app`으로 이동
- **관련 PRD**: 5. 기술 스택 (Firebase Authentication, 로그인/인증 전용)

### 3.3 회원가입 페이지 (`/signup`)

- **목적**: 신규 사용자 계정 생성
- **접근 조건**: 미로그인 사용자만 접근
- **UI 구성요소**
  - 이메일 입력란
  - 비밀번호 입력란 / 비밀번호 확인 입력란
  - "가입하기" 버튼
  - "로그인" 페이지 이동 링크
- **기능**
  - 이메일/비밀번호 기반 계정 생성 (Firebase Authentication)
  - 입력값 유효성 검사 (이메일 형식, 비밀번호 길이/조건, 비밀번호 확인 일치 여부)
  - 중복 이메일 등 가입 실패 시 에러 메시지 표시
  - 가입 성공 시 자동 로그인 후 `/app`으로 이동
- **관련 PRD**: 5. 기술 스택 (Firebase Authentication)

### 3.4 비밀번호 재설정 페이지 (`/reset-password`)

- **목적**: 비밀번호를 잊은 사용자의 계정 복구
- **접근 조건**: 미로그인 사용자만 접근
- **UI 구성요소**
  - 이메일 입력란
  - "재설정 메일 보내기" 버튼
  - "로그인" 페이지 이동 링크
- **기능**
  - Firebase Authentication 비밀번호 재설정 메일 발송
  - 처리 결과(메일 발송 성공/실패) 안내 메시지 표시
- **관련 PRD**: 5. 기술 스택 (Firebase Authentication)

### 3.5 메인 도구 페이지 (`/app`) — ★핵심 페이지

- **목적**: 공학용 계산기와 실험 데이터 통계 분석 기능을 제공하는 서비스의 핵심 페이지
- **접근 조건**: 로그인 필요 (미로그인 시 `/login`으로 리다이렉트)
- **레이아웃**: 상단(계산기) → 중단(데이터 입력 테이블) → 하단(산출 결과) 순서의 단일 스크롤 페이지

#### 3.5.1 상단 — 공학용 계산기 영역
- 사칙연산, 삼각함수, 로그/지수, 거듭제곱/제곱근, 팩토리얼, 상수(π, e), 괄호, 메모리(M+, M-, MR, MC)
- 마우스 클릭 입력 + 키보드 입력 동시 지원
- 계산기 결과를 테이블에 자동 삽입하는 기능은 없음 (v2 예정, 사용자가 직접 확인 후 입력)
- **관련 PRD**: 4.1

#### 3.5.2 중단 — 데이터 입력 테이블 영역
- 2열(X, Y) 구조, 세로 스크롤 지원 테이블
- 행 추가/삭제
- 셀 단위 입력, Tab/Enter 키보드 이동
- 엑셀/스프레드시트 복사 데이터의 다중 행·2열 붙여넣기 지원 (행 부족 시 자동 행 추가)
- 잘못된 값(비숫자, 빈 셀) 입력 시 시각적 경고
- "확인" 버튼 클릭 시 하단 결과 영역 산출 트리거 (자동 실시간 계산 아님)
- **관련 PRD**: 4.2

#### 3.5.3 하단 — 산출 결과 영역
- **필수 산출 항목**: 회귀식, LOD, LOQ, Recovery, 신뢰구간, 회귀 분산분석표(ANOVA), p-값, t-통계량, 구간 그림, 개별값 그림, 상자 그림, 잔차 그림
- **추가 산출 항목**: R², 수정된 R², 잔차표준편차(Syx) 및 표준오차, 예측구간 및 신뢰대역 산점도, Lack-of-fit test, 등분산성 검정(F-test/Levene's test), Shapiro-Wilk 정규성 검정, Grubbs' test 및 Cook's distance 이상치 검정, %RSD, Pearson 상관계수
- **복사/내보내기**
  - 수치·통계표: 복사 버튼 제공 (서식 있는 표 + plain text)
  - 수식: 텍스트 및 이미지 복사 지원
  - 그래프: PNG 이미지 복사/다운로드
- 가중회귀(1/x, 1/x²), 전체 리포트 CSV/파일 내보내기는 제공하지 않음 (v2 예정)
- **관련 PRD**: 4.3, 4.4

### 3.6 404 Not Found 페이지 (`/*`)

- **목적**: 정의되지 않은 경로 접근 시 안내
- **접근 조건**: 없음
- **기능**
  - "페이지를 찾을 수 없습니다" 안내 메시지
  - 로그인 상태에 따라 `/app` 또는 `/login`으로 이동하는 버튼 제공

---

## 4. 공통 요소 (모든 인증 후 페이지)

- **헤더/네비게이션**: 서비스 로고, 로그아웃 버튼
- **인증 가드**: `/app`은 미로그인 시 접근 불가, 로그인 페이지들은 로그인 상태에서 접근 불가 (자동 리다이렉트)
- **반응형 UI**: 데스크톱 우선 (PRD 6. 비기능 요구사항 참고)

## 5. v2 예정 페이지 (참고용, 본 문서 범위 아님)

| 페이지 | 설명 |
|---|---|
| 마이페이지 / 입력 이력 | 사용자별 입력 데이터 및 분석 결과 저장/불러오기 (Firestore) |
| 다중 시리즈 비교 페이지 | 여러 검량선(데이터셋) 동시 비교 |
| 계정 설정 | 비밀번호 변경, 계정 탈퇴 등 |

---

*본 문서는 `PRD.md`와 함께 관리하며, PRD 변경 시 본 문서도 함께 업데이트한다.*
